# 功能卡：弟子谱卷轴档案 UI 重构

- 功能名：弟子谱卷轴档案 UI 重构
- 任务 ID：`DL-043`
- 目标（玩家价值）：让弟子谱从现代卡片式面板进一步收敛为“宗门卷册 + 宣纸档案”的修仙档案页，更贴近题材氛围并提高角色阅读沉浸感。
- 飞轮环节：人口繁衍 → 职司分化 → 反哺宗门
- 依赖：`DiscipleProfile.cs`、`DiscipleRosterSystem.cs`、`DisciplePanel.cs`、`MainDisciplePanel.cs`、`WorldPanel.tscn`、`C:/Users/misoda/Downloads/ai_studio_code (10).html`

## 交付范围

- 保留弟子谱既有入口、筛选、排序与地图联动；
- 左侧名册改为纵向卷册风，按峰脉 / 册录分组显示弟子条目；
- 右侧详情改为宣纸档案页，顶部显示姓名、骨龄、册录信息与灵根圆环；
- 中部改为“根基罗盘 + 修为进度 / 战力印鉴 / 气海储备”双栏结构；
- 底部改为“性情印记 + 衍天批注”两段式信息区。

## 完成标准（DoD）

- [x] 弟子谱整体布局切到卷轴档案风；
- [x] 左侧名册支持分组卷册与当前选中高亮；
- [x] 右侧详情支持灵根圆环、根基罗盘、修为 / 战力 / 气海状态；
- [x] 特征以印记形式展示，建议改为衍天批注文案；
- [x] 筛选、排序、地图联动与弹窗关闭链路保持可用；
- [x] `docs/02_system_specs.md`、`docs/05_feature_inventory.md`、`docs/08_development_list.md` 已同步；
- [x] `dotnet build .\Finally.sln` 通过。

## 二轮补强（侧栏经脉 + 卷宗双栏）

- 背景：上一版弟子谱已卷轴化，但左侧名册层级和右侧信息区仍偏“技术控件拼接感”，与新参考图中的“册页分栏”尚有差距。
- 优化目标：
  - 弹窗骨架收敛到“标题横栏 + 左侧名册侧栏 + 右侧卷宗详情”；
  - 左侧顶部保留双筛选控件，名册树继续支持按峰脉/堂口/条线/册级展开；
  - 右侧统一为“人物抬头 + 根基图谱 + 修为/气海/战力卡 + 性情印记 + 衍天批注”五段；
  - 颜色与材质继续使用宣纸水墨 + 朱砂强调，不引入现代高饱和配色。
- 验收补充：
  - [x] 版式已切为“左侧经脉式名册 + 右侧卷宗详情”，并保留地图联动打开弟子定位；
  - [x] 右侧战力区改为独立评定卡，修为与气海分离显示，阅读顺序更接近参考图；
  - [x] 弟子数据来源仍只读 `DiscipleRosterSystem` 派生结果，不改 `GameLoop` 与存档结构；
  - [x] `dotnet build .\Finally.sln` 通过。

## 三轮补强（侧边栏导航 + 分页卷宗）

- 背景：弟子谱信息项多，单页长卷阅读负担仍较重，需要更清晰的层级拆分。
- 优化目标：
  - 左侧改为固定导航栏：`峰内名录 / 人物档案 / 根基修为 / 印记批注`；
  - 右侧改为分卷滚动区，切换导航时内容独立滚动并回到卷首；
  - 名录页保留筛选、排序和名册树；其他页按主题聚合当前弟子信息。
- 验收补充：
  - [x] 左侧导航可切换分卷并高亮当前分组；
  - [x] 名录页仍支持筛选/排序与名册树选择；
  - [x] 根基修为页集中展示雷达图、境界、气海与战力评定；
  - [x] 印记批注页保留性情印记与衍天批注；
  - [x] `dotnet build .\Finally.sln` 通过。

## 四轮补强（同页全展开）

- 背景：玩家希望“峰内名录 / 人物档案 / 根基修为 / 印记批注”在同一页完整展开，不再分页切换。
- 优化目标：
  - 弟子谱弹窗扩大到接近全屏画幅；
  - 左侧保留名录与筛选，右侧按顺序堆叠人物档案、根基修为、印记批注三段；
  - 取消分页切换逻辑，避免信息被隐藏。
- 验收补充：
  - [x] 四段信息同页可见，且不依赖分页切换；
  - [x] 名录、筛选、详情联动仍可用；
  - [x] `dotnet build .\Finally.sln` 通过。

## 五轮补强（宽屏仪表盘）

- 背景：宽屏模式下内容被线性拉伸，横向留白过大，阅读疲劳。
- 优化目标：
  - 右侧改为双列仪表盘：左列“先天根基（雷达图+属性块）”，右列“修为与造化（修为条+气海条+战力签+印记）”；
  - 属性改为 3x2 小块阵列，避免长条横向拉伸；
  - 名录树降为“峰脉分组 + 弟子列表”两层，减少深层折叠；
  - 弹窗继续保持近全屏画幅。
- 验收补充：
  - [x] 双列仪表盘布局可用，雷达图与属性块不过度拉伸；
  - [x] 战力评定收敛为“朱砂签”样式，不再是大红框；
  - [x] 名录层级缩减为两层，筛选/排序仍可用；
  - [x] `dotnet build .\Finally.sln` 通过。
