# 功能卡（Feature Card）

## 功能信息

- 功能名：HTML 参考稿 UI 等比重构
- 优先级：`P1`
- 目标版本：`v0.x`
- 关联系统：`UI`

## 目标与用户收益

- 目标：将 Godot 主界面视觉风格、控件布局与信息分区对齐到 `C:/Users/shawn/Downloads/ai_studio_code (13).html` 与参考图。
- 玩家可感知收益（10 分钟内）：主界面视觉统一、信息层级更清晰、操作入口与参考稿一致。

## 实现范围

- 包含：
  - 顶栏资源组、纪元区、结算进度与倒计时文案重排
  - 左栏“吏部·百工调度”结构与岗位卡样式重构
  - 中栏“宏观视界 + 底部基建流”布局重构
  - 右栏“军情急报 + 郡县志日志”布局与样式重构
  - 底栏“速度控制 + 系统按钮”布局重构
  - 同步更新 `Main.cs` 节点绑定路径与文案
- 不包含：
  - 新增玩法系统/公式
  - 新增建筑真实升级机制
  - 存档格式与核心循环逻辑改动

## 实现拆解

1. 使用 `StyleBoxFlat` 重建深色新中式基础主题
2. 按 HTML 分区改造 `Main.tscn` 与 `scenes/ui/*.tscn`
3. 调整 `Main.cs` 绑定路径与 UI 刷新逻辑，保证已有交互不回归

## 验收标准（可测试）

- [ ] 顶/左/中/右/底五大区域与参考图层级一致
- [ ] 资源栏、纪元进度条、岗位卡、警报卡、基建卡均按参考稿样式呈现
- [ ] 岗位加减、地图切换、探险开关、存档/读档仍可用
- [ ] `dotnet build .\Finally.sln` 通过

## 风险与回滚

- 风险：节点树重排导致路径失配、旧主题残留造成样式不统一
- 回滚方式：回退 `scenes/Main.tscn`、`scenes/ui/*.tscn`、`scripts/Main.cs`、`themes/HanCourtyardTheme.tres`
