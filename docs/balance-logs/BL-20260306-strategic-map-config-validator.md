# 数值平衡日志（Balance Log）

## 记录信息

- 日期：2026-03-06
- 版本：v0.x
- 关联提案：无（本次为配置合法性校验增强）

## 改动摘要

- 改动项：战略地图配置启动校验（点数/坐标/颜色）与告警日志输出
- 改动前：配置异常仅在渲染效果上间接暴露，缺少明确错误提示
- 改动后：加载阶段即输出 `GD.PushWarning`，并对非法字段使用安全回退值

## 结果数据

- 指标 1：`dotnet build .\Finally.sln` 通过（0 warning / 0 error）
- 指标 2：区域点数、线段点数、颜色格式、坐标超界均可输出定位信息
- 指标 3：非法宽度/半径字段会重置到可渲染值，避免地图不可见

## 结论

- 是否达到预期：`是`
- 下一步：`保留`

## 复盘

- 有效原因：问题在加载期暴露，降低运行时盲查成本
- 无效原因：当前仅告警不阻断，仍可能带着问题配置进入运行
- 后续假设：后续可引入“严格模式”在严重错误时拒绝加载并回退整张地图
