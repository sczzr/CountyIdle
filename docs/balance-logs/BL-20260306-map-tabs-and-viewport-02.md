# 数值平衡日志（Balance Log）

> 历史兼容说明（2026-03-08）：本文保留旧阶段的“县城 / 郡图 / 郡县 / 州府”等表述以便追溯；在当前项目语义中，请分别按“天衍峰驻地 / 江陵府外域 / 天衍峰经营 / 世界地图”理解。


## 记录信息

- 日期：2026-03-06
- 版本：v0.x
- 关联提案：无（本次为 UI 结构与切页交互）

## 改动摘要

- 改动项：`MapViewport` 中央 fill 化 + `TopTabRow` 统一切换多内容页（县城/郡图/州域/事件/报表/探险）
- 改动前：中部区域同时混杂地图与下方卡片区，`TopTabRow` 仅限地图层切换，中心承载不统一
- 改动后：中部仅保留 `TopTabRow + MapViewport`，所有内容页都在 `MapViewport` 内切换；产业卡迁移到“统计报表”页

## 结果数据

- 指标 1：`dotnet build .\Finally.sln` 通过（0 warning / 0 error）
- 指标 2：`Main.cs` 页签状态与页面显隐一一对应，避免多面板同时显示
- 指标 3：产业升级按钮能力保留（农坊/工坊/矿坑路径迁移后仍可绑定）

## 结论

- 是否达到预期：`是`
- 下一步：`保留`

## 复盘

- 有效原因：将中部内容统一收敛到单视口，切页逻辑清晰且可扩展
- 无效原因：事件/探险/报表仍为占位内容，尚未接入真实业务数据
- 后续假设：后续新增内容类型只需在 `MapPages` 增页并接入一枚 tab，不需再改整体布局


