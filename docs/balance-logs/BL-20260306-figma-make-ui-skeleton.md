# 数值平衡日志（Balance Log）

## 记录信息

- 日期：2026-03-06
- 版本：v0.x
- 关联提案：无（本次为 UI 骨架落地）

## 改动摘要

- 改动项：新增 Figma Make 对应主界面骨架子场景与脚本
- 改动前：仅存在当前生产主界面结构，缺少 Figma Make 对应的独立骨架
- 改动后：新增 `HUDTopBar / EquipmentPanel / CenterView / TimelinePanel / BottomBar` 与组合场景 `MakeGameUI`

## 结果数据

- 指标 1：`dotnet build .\Finally.sln` 通过（0 error, 0 warning）
- 指标 2：核心小时结算规则未改动（仍为每 60 游戏分钟）
- 指标 3：存档/读档与探险开关逻辑无改动（`Main.cs` 绑定路径未替换）

## 结论

- 是否达到预期：`是`
- 下一步：`保留`

## 复盘

- 有效原因：采用独立目录和组合场景接入，避免影响现有生产主场景节点绑定
- 无效原因：当前为 UI 骨架占位，尚未接入实时状态与完整交互
- 后续假设：后续将新骨架按模块逐步映射到 `Main.cs` 数据发布层，可降低一次性替换风险
