# 数值平衡日志（Balance Log）

## 记录信息

- 日期：2026-03-06
- 版本：v0.x
- 关联提案：`docs/change-proposals/CP-20260306-shortcut-config-system.md`

## 改动摘要

- 改动项：设置面板增加快捷键配置并接入主界面快捷键触发
- 改动前：操作仅依赖按钮点击，快捷键不可配置
- 改动后：支持配置并持久化 7 个高频操作快捷键，运行时即时生效

## 结果数据

- 指标 1：`ClientSettings` 新增 7 个快捷键字段并带默认值
- 指标 2：`SettingsPanel` 支持快捷键下拉重绑定
- 指标 3：`MainShortcutBindings.cs` 接入快捷键触发逻辑（设置/仓储/探险/倍速/存读档/重置）
- 指标 4：`dotnet build .\Finally.sln` 通过（0 错误 / 0 警告）

## 结论

- 是否达到预期：`是`
- 下一步：`保留`

## 复盘

- 有效原因：高频交互从纯鼠标切换为鼠标 + 键盘双通路，操作成本下降。
- 无效原因：本次未支持组合键与冲突即时提示，进阶玩家定制度仍有限。
- 后续假设：若玩家需要更自由绑定，可升级为按键捕获式重绑定而不是下拉选择。
