# 数值平衡日志：历史归档语义桥接（V1）

## 记录信息

- 日期：2026-03-08
- 版本：当前工作区
- 关联提案：`无（文档治理任务）`

## 改动摘要

- 改动项：为历史归档补齐兼容说明，并为高频旧链路追加“当前对外语义”，继续把 `countytown-* / prefecture-*` 旧归档全文更新到宗门 / 外域语义。
- 改动前：旧 `FC / CP / BL` 虽然保留了历史价值，但打开正文后仍常以“县城 / 郡图 / 郡县 / 州府”视角叙述，当前阅读需要自行做语义翻译。
- 改动后：旧归档开头统一带有“历史兼容说明”，关键链路额外标注“当前对外语义”，代表性文档的标题、目标与摘要层同步改成宗门 / 外域表达；`countytown-* / prefecture-*` 链路的功能名、提案标题、平衡日志改动项、实现范围、拆解步骤、验收项与结果描述也完成了宗门化收口。

## 结果数据

- 指标 1：批量补“历史兼容说明”的旧归档数 = `79`
- 指标 2：补“当前对外语义”与摘要层宗门化的代表性旧归档数 = `18`
- 指标 3：`countytown-* / prefecture-*` 目标旧归档数 = `41`，其中补齐“当前对外语义”头注数 = `41`
- 指标 4：`countytown-* / prefecture-*` 目标旧归档漏检结果 = `NO_HITS`
- 指标 5：`dotnet build .\Finally.sln` 通过（0 warning / 0 error）

## 结论

- 是否达到预期：`是`
- 下一步：`保留`

## 复盘

- 有效原因：不去破坏历史 `canonical-topic` 与正文追溯链路，只在开头增加“如何按当前语义阅读”的桥接层，收益高、风险低。
- 无效原因：大量旧正文深层段落仍保留历史时期描述，若后续需要公开展示旧归档，仍建议继续做摘要页或专题汇总。
- 后续假设：下一轮若继续整理，可把剩余非 `countytown-* / prefecture-*` 的历史专题也按同样方式做全文语义桥接。
