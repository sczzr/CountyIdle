# 数值平衡日志：江陵府外域备用视图运营语义统一（V2）

## 记录信息

- 日期：2026-03-08
- 版本：当前工作区
- 关联提案：`docs/change-proposals/CP-20260308-outer-region-operational-semantics-v2.md`

## 改动摘要

- 改动项：将隐藏备用外域视图的运营提示、调度按钮与标题 fallback 统一切到修仙外域语义。
- 改动前：江陵府外域备用视图虽然已经改名，但状态提示仍使用 `驿路 / 乡里 / 郡路`，标题 fallback 仍可能显示 `宗门外域`。
- 改动后：江陵府外域备用视图统一使用 `灵道 / 附庸据点 / 抚恤附庸 / 峰外采办 / 江陵府外域` 术语。

## 结果数据

- 指标 1：残留旧词 `驿路 / 乡里 / 郡路 / 宗门外域` 在当前代码与主规格文档中已清零
- 指标 2：覆盖文件数 = `7` 个代码/文档文件 + `3` 份归档文档
- 指标 3：`dotnet build .\Finally.sln` 通过

## 结论

- 是否达到预期：`是`
- 下一步：`保留`

## 复盘

- 有效原因：这一轮只收运营提示和 fallback 文案，属于高可见、低风险的主题一致性补强。
- 无效原因：内部仍保留 `Prefecture` 和 `ReliefVillages` 等旧代码名，后续若要彻底主题化，还需更深层抽象。
- 后续假设：下一轮可继续把江陵府外域备用视图的节点类型名、颜色主题和可能回开的事件文案也抽成可切换主题规则。


