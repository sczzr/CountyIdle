# 数值平衡日志（Balance Log）

## 记录信息

- 日期：2026-03-06
- 版本：v0.x
- 关联提案：`CP-20260306-ui-interaction-rewire.md`

## 改动摘要

- 改动项：主界面（旧布局 + Figma 布局）按钮事件补齐，`x2` 速度改为真实时间倍率
- 改动前：Figma 主界面存在可见按钮但无玩法事件；`x2` 按钮仅文字占位
- 改动后：按钮可触发探险、倍速、存读档、重置、扩建与制工具；`x2` 可真实加速分钟推进

## 结果数据

- 指标 1：`dotnet build .\Finally.sln` 通过（0 error, 0 warning）
- 指标 2：`GameLoop` 结算仍按每 60 游戏分钟触发（仅改变现实秒到游戏分钟换算）
- 指标 3：Figma 布局新增 8 个已绑定交互入口（探险、倍速、存档、读档、重置、扩建农坊、扩建工坊、锻造工具）

## 结论

- 是否达到预期：`部分`
- 下一步：`保留并继续调参`

## 复盘

- 有效原因：复用既有 `GameLoop` 与产业接口，避免新依赖即可补齐 UI 交互闭环
- 无效原因：当前环境缺少 Godot 可执行文件，未能完成 `F5` 运行态手点验证
- 后续假设：在本机 Godot 编辑器下补跑按钮手测后，可进一步统一两套布局文案和引导提示
