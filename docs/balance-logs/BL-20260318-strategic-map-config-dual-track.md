# 数值平衡日志（Balance Log）

## 记录信息

- 日期：2026-03-18
- 版本：DL-001（战略地图配置驱动接入）
- 关联提案：CP-20260315-strategic-map-config-dual-track

## 改动摘要

- 改动项：战略地图配置/生成双轨切换的调试入口（UI + 统一切换接口）。
- 改动前：仅可通过 `F8` 在两套定义间切换，缺少显式按钮入口。
- 改动后：世界舆图页新增调试按钮，可与 `F8` 共同切换，并同步世界图与外域图来源。

## 结果数据

- 指标 1：调试入口可见性（Debug 构建）= 待验证
- 指标 2：切换后地图渲染变更 = 待验证
- 指标 3：配置缺失 fallback 仍可运行 = 待验证

## 结论

- 是否达到预期：部分（需要手动验证）
- 下一步：继续调试验证

## 复盘

- 有效原因：调试入口和切换接口统一后便于验证配置渲染。
- 无效原因：未在本轮完成手动验证记录。
- 后续假设：配置版地图可覆盖当前默认生成版的关键内容。
