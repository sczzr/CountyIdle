# 数值平衡日志（Balance Log）

## 记录信息

- 日期：2026-03-06
- 版本：v0.x
- 关联提案：无（本次为地图素材渲染链路调整）

## 改动摘要

- 改动项：县城俯瞰图与周边郡图切换为 `ExtractedTileSet.tres` 参与绘制
- 改动前：
  - 县城图使用分散纹理路径（`chinese_style_seamless`/`modular`）绘制
  - 周边郡图使用纯色底板，不使用提取瓦片集
- 改动后：
  - 县城图从 `ExtractedTileSet` atlas 坐标读取地表与建筑纹理（保留资源缺失回退）
  - 周边郡图增加 `ExtractedTileSet` 瓦片底图铺设

## 结果数据

- 指标 1：`dotnet build .\Finally.sln` 通过（0 warning / 0 error）
- 指标 2：地图缩放接口未变更（`IMapZoomView` 仍由现有逻辑驱动）
- 指标 3：县城图与周边郡图素材来源统一到同一瓦片集链路

## 结论

- 是否达到预期：`是`
- 下一步：`保留`

## 复盘

- 有效原因：在不改动地图页签与交互框架的前提下，直接替换到 TileSet atlas 读取，改动面小且稳定。
- 无效原因：atlas 语义与地形语义仍是人工映射，局部贴图尚未做精调。
- 后续假设：可在后续迭代中将 atlas 坐标映射外置配置化，进一步降低美术替换成本。
