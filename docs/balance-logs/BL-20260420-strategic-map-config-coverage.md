# 数值平衡日志：战略地图配置覆盖补强

## 记录信息

- 日期：2026-04-20
- 版本：DL-001（战略地图配置驱动接入）
- 关联提案：`docs/change-proposals/CP-20260420-strategic-map-config-coverage.md`

## 改动摘要

- 改动项：补强 `strategic_maps.json` 配置覆盖，并让江陵府外域备用图默认使用配置源。
- 改动前：世界图配置缺少标签、河流与节点类型；江陵府外域节点类型不完整；世界图与外域图默认仍偏生成源，仅能通过调试入口切到配置源。
- 改动后：世界图配置覆盖 `regions / outlines / routes / rivers / nodes / labels`；外域图节点补齐 `city / ward / settlement / raw_source / landmark` 类型与地名标签；`PrefectureMapView` 默认走配置源，世界图保留生成源作为主链。

## 结果数据

- 指标 1：世界图配置字段覆盖 = `6/6`（区域、轮廓、路线、河流、节点、标签）。
- 指标 2：外域图节点类型覆盖 = `5` 类（主城、坊区、聚落、原料点、地标）。
- 指标 3：默认切源裁定 = 江陵府外域可默认配置驱动；世界图暂不默认切回，避免丢失 hex 点选、二级地图入口与 L1 地貌贴图链路。
- 指标 4：`dotnet build .\Finally.sln` = 通过（`0 Warning(s)` / `0 Error(s)`）。
- 指标 5：Godot 4.6 headless `res://scenes/ui/WorldPanel.tscn` = 通过（`exit code 0`，无新增解析错误输出）。

## 结论

- 是否达到预期：`部分`
- 下一步：`继续调试验证`

## 复盘

- 有效原因：配置文件已经能承载可读地名、河道、外轮廓与节点类型，外域备用图也形成默认配置驱动入口。
- 无效原因：世界图配置源仍只是静态图层，尚不能替代生成源承载的世界 hex 数据、站点选择与二级地图入口。
- 后续假设：若要让世界图也默认切回配置源，需要先扩展配置模型，使其能描述 hex cell、站点、地貌家族、资源与二级地图入口。
