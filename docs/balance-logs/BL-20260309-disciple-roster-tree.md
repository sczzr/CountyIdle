# 数值平衡日志：弟子谱多层树状名册

- 任务 ID：`DL-043`
- 时间：`2026-03-09`

## 改动前

- 左侧卷册名册虽然已经有题材化外观，但内部仍是单层分组；
- 各峰内部弟子数量一多时，缺少按堂口、册级继续下钻的组织层次；
- 这会影响后续做峰内任命、培养和多层管理时的可扩展性。

## 改动后

- 左侧名册改为真正的 `Tree` 控件；
- 树层级固定为：`峰脉 -> 堂口 / 机构 -> 册级 -> 弟子`；
- 当前筛选与排序结果会重新生成整棵树；
- 叶节点点击后直接刷新右侧宣纸档案，并保持地图联动定位能力。

## 指标结果

- 名册树层级：`4`
- 顶层：`峰脉`
- 中层：`堂口 / 机构`、`册级`
- 叶节点：`弟子`
- 构建结果：`dotnet build .\Finally.sln` 通过

## 结论

这轮仍是表现层与管理结构增强，不改资源、人口、战斗或治理数值。  
它提升的是弟子谱在“组织管理”层面的可扩展性，让峰内弟子不只是可看，而且开始具备真正的层级管理骨架，为后续做执事任命、师承链和峰内编制打下基础。
