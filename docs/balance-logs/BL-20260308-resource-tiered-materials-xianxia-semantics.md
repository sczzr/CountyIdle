# 数值平衡日志（Balance Log）

## 记录信息

- 日期：2026-03-08
- 版本：v0.x（六期：修仙材料语义化）
- 关联提案：`docs/change-proposals/CP-20260308-resource-tiered-materials-xianxia-semantics.md`

## 改动摘要

- 改动项：资源系统分层扩展（V1 六期：修仙材料语义化）
- 改动前：
  - `DL-023` 已完成 `T0/T1` 材料链与整数库存化，但玩家可见名称仍偏通用经营素材语义。
  - 仓储面板、主界面提示、小时结算日志、人口缺口提示与外域原料标签分别维护材料文案，缺少统一入口。
- 改动后：
  - 新增 `MaterialSemanticRules`，集中维护 `27` 个库存字段的玩家可见修仙名称与说明。
  - 仓储面板分类、卡槽名称、描述与按钮提示切到修仙材料语义。
  - `ResourceSystem / IndustrySystem / PopulationSystem / Main / PrefectureMapGeneratorSystem` 的关键玩家可见材料文案统一接入该映射。
  - `GameState`、存档结构与资源公式保持不变。

## 结果数据

- 指标 1：集中维护的玩家可见材料条目数=`0 -> 27`
- 指标 2：统一接入修仙命名的关键展示面=`0 -> 6（仓储/顶部资源/结算日志/制造日志/人口缺口/外域原料标签）`
- 指标 3：新增技术兼容成本=`0（未改 GameState 字段与存档结构）`
- 指标 4：构建验证=`dotnet build .\\Finally.sln` 通过
- 指标 5：存档回归验证=`tools/SaveSmoke/bin/Debug/net8.0/SaveSmoke.exe` 通过（`slot_count=5 -> final_slot_count=4`，成功读取 `autosave_2`，`schema=1`）

## 结论

- 是否达到预期：`部分`
- 下一步：`Godot 烟测`

## 复盘

- 有效原因：将玩家可见命名从散落字符串收敛为集中映射后，仓储、日志和地图语义能一起切到修仙背景，且不触碰资源公式与存档兼容层。
- 无效原因：本轮仍未完成 `Godot` 运行烟测；当前环境未发现 `Godot` 可执行，因此尚未验证所有 UI 场景下的长文本显示与实际操作体验。
- 后续假设：若试玩中发现部分名称过于抽象，可继续沿用集中映射入口，把个别词条调整为“修仙名 + 通用名”的混合显示，而无需回退底层资源链实现。
