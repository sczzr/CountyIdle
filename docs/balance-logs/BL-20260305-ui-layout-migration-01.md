# 数值平衡日志（Balance Log）

## 记录信息

- 日期：2026-03-05
- 版本：v0.x
- 关联提案：无（本次为 UI 布局与场景拆分）

## 改动摘要

- 改动项：将主界面按 RimWorld 风格拆分为多场景组合布局
- 改动前：主界面 UI 结构集中在单个 `Main.tscn`
- 改动后：拆分为 `TopBar` / `JobsPanel` / `WorldPanel` / `EventLogPanel` / `BottomBar` / `JobRow`

## 结果数据

- 指标 1：小时结算频率（每 60 游戏分钟）不变
- 指标 2：岗位分配与空闲人口约束逻辑不变
- 指标 3：探险开关、存档/读档流程入口不变

## 结论

- 是否达到预期：`是`
- 下一步：`保留`

## 复盘

- 有效原因：UI 文件所有权更清晰，后续可独立迭代各面板
- 无效原因：无
- 后续假设：可进一步为 `JobRow` 增加独立脚本和主题资源以支持视觉扩展
