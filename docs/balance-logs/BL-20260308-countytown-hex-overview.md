# 数值平衡日志（Balance Log）

## 记录信息

- 日期：`2026-03-08`
- 版本：`v0.x`
- 关联提案：`docs/change-proposals/CP-20260308-countytown-hex-overview.md`

## 改动摘要

- 改动项：将县城俯瞰主视图从等距地表投影补强为 hex tile 俯视地表
- 改动前：县城主视图使用等距格位中心、菱形地表 tile，居民、场所与建筑均围绕等距投影渲染
- 改动后：县城主视图改用 hex tile 地表坐标中心与六边形格位；居民路线、场所选中、建筑 / 场所 overlay 与地图提示同步跟随新的 hex 俯视投影

## 结果数据

- 指标 1：县城主视图投影类型 = `hex tile 俯视`
- 指标 2：继续复用的生成链 = `TownMapGeneratorSystem + TownMapData`
- 指标 3：受影响的核心结算系统 = `0`（仅表现层）
- 指标 4：`dotnet build .\Finally.sln` = `通过（0 warning / 0 error）`

## 结论

- 是否达到预期：`是`
- 下一步：`保留`

## 复盘

- 有效原因：只替换县城地图的视图坐标与地表几何，就能让建筑、场所与居民整体跟着切到 hex 俯视，而不用重写生成逻辑。
- 无效原因：这轮保留了现有 overlay 建筑表现，若后续要更强的六角地块一致性，还可以继续把建筑 footprint 也专门做成 hex 风格。
- 后续假设：若用户继续推进县城 hex 化，可在下一轮把建筑轮廓、道路纹理与场所底座进一步收敛到更统一的 hex 语义。
