# 数值平衡日志（Balance Log）

## 记录信息

- 日期：2026-03-06
- 版本：v0.x
- 关联提案：无（本次为 UI 自适应修复）

## 改动摘要

- 改动项：修复窗口缩小时 UI 溢出与覆盖
- 改动前：中区建筑卡会越界覆盖右栏与底栏
- 改动后：中区卡片自动换行，面板超界绘制被裁剪，三栏宽度更可压缩

## 结果数据

- 指标 1：窗口缩小后核心交互按钮仍可点击
- 指标 2：岗位调整、探险开关、存档/读档、重置逻辑不变
- 指标 3：小时结算频率仍为每 60 游戏分钟

## 结论

- 是否达到预期：`是`
- 下一步：`保留`

## 复盘

- 有效原因：布局容器从固定横排改为流式换行，配合裁剪消除跨区溢出
- 无效原因：极小窗口仍会有内容被裁剪，后续可加滚动容器进一步优化
- 后续假设：若继续兼容更小分辨率，可在左栏与中区加入 `ScrollContainer`
