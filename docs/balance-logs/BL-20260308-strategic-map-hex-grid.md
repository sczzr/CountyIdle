# 数值平衡日志（Balance Log）

> 历史兼容说明（2026-03-08）：本文保留旧阶段的“县城 / 郡图 / 郡县 / 州府”等表述以便追溯；在当前项目语义中，请分别按“天衍峰驻地 / 江陵府外域 / 天衍峰经营 / 世界地图”理解。


## 记录信息

- 日期：`2026-03-08`
- 版本：`v0.x`
- 关联提案：`docs/change-proposals/CP-20260308-strategic-map-hex-grid.md`

## 改动摘要

- 改动项：把世界图 / 郡图的战略视图从方格辅助线补强为 hex 战略底格与六角节点标记
- 改动前：`StrategicMapViewSystem` 以矩形网格线作为背景辅助，世界图 / 郡图虽然已可缩放并支持配置 / 生成叠层，但 hex 俯视感较弱
- 改动后：背景改为按 `grid_lines` 密度绘制的六角底格，默认节点改为六角标记；区域、路线、河流、标签、高倍率郡城细节与经营态染色链路继续保留

## 结果数据

- 指标 1：hex 底格适用视图数 = `2`（`World / Prefecture`）
- 指标 2：复用原有数据输入数 = `2`（`strategic_maps.json world` + `PrefectureMapGeneratorSystem`）
- 指标 3：主循环 / 存档影响 = `0`（仅表现层）
- 指标 4：`dotnet build .\Finally.sln` = `通过（0 warning / 0 error）`

## 结论

- 是否达到预期：`是`
- 下一步：`保留`

## 复盘

- 有效原因：直接在 `StrategicMapViewSystem` 视图层替换底格，可以同时覆盖世界图与郡图，且不碰 `Main.cs` 和数据结构。
- 无效原因：这轮仍是“hex 风格战略视图”，不是完整逐格地图系统；后续若要做真正逐格地貌编辑，还需补地块语义与格位分类层。
- 后续假设：若用户继续沿 hex 地图推进，可优先在郡图上补“道路 / 河流 / 原料点”的 hex 语义缓存，再决定是否推广到世界图。


