# 数值平衡日志（Balance Log）

## 记录信息

- 日期：2026-03-12
- 版本：当前开发版
- 关联提案：`CP-20260312-world-cell-secondary-map-runtime-click`

## 改动摘要

- 改动项：打通世界地图全格二级地图入口，并让进入按钮生成按格语义驱动、与山门沙盘同形的局部沙盘
- 改动前：只有世界站点 `Sites` 可点入二级地图占位页，普通 world hex 只是背景地貌；即使进入占位页，也还没有真正生成地图。
- 改动后：世界图点击改为 `站点优先、地块回退`，普通 hex 会先在左侧显示详情；玩家再通过进入按钮打开 `SecondaryMapView`，并看到一张按该格语义生成、但表现形态与山门沙盘一致的局部 hex 沙盘。

## 结果数据

- 指标 1：世界格入口覆盖 = `全部 world hex`
- 指标 2：格子级新增主类型 = `Wilderness`
- 指标 3：运行时入口链路 = `世界图点击 -> 左侧详情 -> 进入按钮 -> SecondaryMapView 同形沙盘 -> 返回世界图`

## 结论

- 是否达到预期：`是`
- 下一步：`保留`

## 复盘

- 有效原因：先复用已有 `SelectedWorldSite + SecondaryMapView` 链路，并复用山门沙盘同族视图，把缺口集中在命中、语义回退和地图内容生成层，没有同时重做整张 UI 页。
- 无效原因：本轮仍未把格子级入口接到专属场景、事件或结算上，当前二级沙盘仍以模板化局部地势和基础地块信息为主。
- 后续假设：先把 `Wilderness / Sect / Market / Ruin` 四类模板继续做实，再将这张同形沙盘逐步接到专属玩法，比先全量铺七类独立场景更稳。
