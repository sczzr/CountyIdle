# 数值平衡日志（Balance Log）

## 记录信息

- 日期：2026-03-06
- 版本：v0.x
- 关联提案：无（本次为 UI 接入切换）

## 改动摘要

- 改动项：`Main.tscn` 挂载 Figma 组合场景，并新增 `UseFigmaLayout` 回滚开关
- 改动前：`Main` 仅存在既有布局，无法在主场景低风险预览 Figma 布局
- 改动后：可在 `Main` Inspector 通过 `UseFigmaLayout` 在“既有布局 / Figma 布局”间切换

## 结果数据

- 指标 1：`dotnet build .\Finally.sln` 通过（0 error, 0 warning）
- 指标 2：小时结算节奏保持每 60 游戏分钟（未改 `GameLoop`）
- 指标 3：存档/读档、探险开关逻辑保持可用（旧布局路径保留）

## 结论

- 是否达到预期：`是`
- 下一步：`保留`

## 复盘

- 有效原因：通过显示层开关接入新布局，不触发玩法层与存档结构风险
- 无效原因：Figma 布局目前仍为展示骨架，尚未接入实时数据发布
- 后续假设：可分模块逐步把顶部资源栏、底部按钮等绑定迁移到 Figma 场景
